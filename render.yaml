services:
  - type: web
    name: corrosion-backend
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app --preload --workers=1 --bind=0.0.0.0:$PORT
    region: singapore
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.13"